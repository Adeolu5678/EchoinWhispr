{
  "name": "echoinwhispr-monorepo",
  "version": "1.0.0",
  "description": "EchoinWhispr - Anonymous messaging platform monorepo",
  "private": true,
  "packageManager": "pnpm@8.15.0",
  "workspaces": [
    "Web",
    "ReactNative",
    "Convex",
    "design-system"
  ],
  "scripts": {
    "install:all": "pnpm install",
    "dev": "pnpm -r dev",
    "dev:web": "pnpm --filter Web dev",
    "dev:rn": "pnpm --filter ReactNative start",
    "dev:convex": "pnpm --filter Convex dev",
    "start:web": "pnpm --filter Web start",
    "start:rn": "pnpm --filter ReactNative start",
    "build": "pnpm -r build",
    "build:web": "pnpm --filter Web build",
    "build:rn": "pnpm --filter ReactNative build",
    "build:convex": "pnpm --filter Convex deploy",
    "build:all": "pnpm build:web && pnpm build:rn && pnpm build:convex",
    "lint": "pnpm -r lint",
    "lint:fix": "pnpm -r lint:fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "type-check": "pnpm -r type-check",
    "test": "pnpm -r test",
    "clean": "pnpm -r clean",
    "clean:node_modules": "if exist node_modules rmdir /s /q node_modules && for /d /r . %d in (node_modules) do @if exist \"%d\" rmdir /s /q \"%d\" && del /q pnpm-lock.yaml",
    "clean:install": "pnpm clean:node_modules && pnpm install",
    "convex:dev": "pnpm --filter Convex dev",
    "convex:deploy": "pnpm --filter Convex deploy",
    "convex:generate": "pnpm --filter Convex generate",
    "setup": "pnpm install && husky install",
    "postinstall": "husky install"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "husky": "^8.0.0",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0",
    "eslint": "^8.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/your-username/echoinwhispr.git"
  },
  "keywords": [
    "monorepo",
    "react",
    "react-native",
    "nextjs",
    "convex",
    "typescript"
  ],
  "author": "EchoinWhispr Team",
  "license": "MIT"
}